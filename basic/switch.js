const age = 27;

switch (true) {
  case age < 25:
    console.info("Your still young.");

  case age >= 25:
    console.info("You already old.");

  default:
    console.info("We don't know what is your age.");
}
